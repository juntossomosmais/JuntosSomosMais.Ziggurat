name: Publish coverage report

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'src/**'
      - 'tests/**'
      - '*.sln'
      - '**/Directory.Build.props'
  workflow_dispatch:

jobs:
  publish-coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '8.0.x'
      - name: Install all tools
        run: |
          dotnet tool restore
      - name: Check if the project is well formatted
        run: |
          ./scripts/start-check-formatting.sh
      - name: Build the project, run all tests and publish to SonarCloud
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          PROJECT_VERSION: ${{ github.sha }}
          SOURCE_BRANCH_NAME: ${{ github.ref_name }}
        run: |
          set -e
          docker compose up --detach --wait --wait-timeout 120 --remove-orphans sqlserver
          docker compose up --detach --wait --wait-timeout 120 --remove-orphans mongoclustersetup
          ./scripts/start-sonarcloud.sh
